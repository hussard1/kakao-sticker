import BoardView from './views/BoardView'
import Posts from './models/Posts'
import controller from './controller/Controller'

// new BoardController(new Posts(), new BoardView())

// new BoardView(new Posts())


new controller()

